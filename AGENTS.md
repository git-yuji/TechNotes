# Tech Notes 作業ルール

## 使用言語

Codexからユーザーへの回答、作業報告、コードレビュー、レビューコメント、
問題点の指摘、修正提案は、すべて日本語で記述する。

コード、コマンド、ファイル名、変数名、関数名、ライブラリ名、
エラーメッセージなど、正確性のために原文が必要なものは英語のままでよい。
英語のエラーメッセージや専門用語を示す場合は、必要に応じて日本語で意味を説明する。

## ブランチ運用

ユーザーから新しい作業指示を受けた場合は、ファイルを変更する前に現在の
ブランチと作業ツリーの状態を確認する。

作業を開始する前に、必ず次のいずれかを行う。

- 作業内容に合うブランチを新しく作成する、または既存の作業ブランチへ切り替える
- 使用するブランチ名をユーザーへ提示する

`main`ブランチ上では、ファイルの変更やコミットを直接行わない。
`main`ブランチにいる場合は、作業内容に合うブランチへ切り替えてから変更する。

ブランチ名は、変更内容に応じて次の接頭辞を使用し、内容が分かる名前にする。

- `feat/`: 機能の追加
- `fix/`: 不具合の修正
- `docs/`: ドキュメントの追加・更新
- `style/`: 見た目やレイアウトの変更
- `refactor/`: 挙動を変えないコードの整理
- `test/`: テストの追加・修正
- `chore/`: 設定や依存関係などの保守作業

現在のブランチで関連する作業を継続する場合も、変更前にそのブランチを
使用することをユーザーへ伝える。

## コミットメッセージ

コミットメッセージは Conventional Commits を基本とし、次の形式で統一する。

```text
<英語の接頭辞>: <日本語の説明>
```

接頭辞は必ず英語の小文字で記述し、コロンの後に半角スペースを1つ入れる。
説明は日本語で簡潔に記述する。

### 接頭辞

- `feat`: 機能の追加
- `fix`: 不具合の修正
- `chore`: 環境設定、依存関係の更新、その他の保守作業
- `docs`: ドキュメントの追加・更新
- `style`: 見た目やレイアウトの変更
- `refactor`: 挙動を変えないコードの整理
- `test`: テストの追加・修正

### 例

```text
feat: ノート一覧画面を追加
fix: タグが重複登録される問題を修正
chore: Next.jsとCloudflare Workersの開発環境を構築
docs: READMEに開発手順を追記
style: ノートカードのレイアウトを調整
refactor: ノート取得処理を整理
test: ノート登録処理のテストを追加
```

コミットを提案または作成するときは、変更内容に最も適した接頭辞を選ぶ。
ユーザーから明示的に依頼されない限り、コミットやプッシュは実行しない。

## Pull Request

Pull Requestのタイトルと本文は日本語で記述する。
タイトルは変更全体を簡潔に表し、本文は実際の差分に基づいて作成する。

本文には原則として次の内容を含める。

- 変更内容
- 変更した理由・目的
- 影響範囲
- 動作確認
- 補足事項

`.github/pull_request_template.md` の構成を使用し、該当しない項目には
「なし」または該当しない理由を記載する。テンプレートの見出しだけを残した
空のPull Request本文を作成しない。

ユーザーから明示的に依頼されない限り、Pull Requestの作成やマージは実行しない。
